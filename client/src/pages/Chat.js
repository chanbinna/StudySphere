import React from 'react';
import { NavBar } from '../components/NavBar';

export const Chat = () => {
    return (
        <div>
            <NavBar />
            <h1>Chat</h1>
        </div>
    );
}