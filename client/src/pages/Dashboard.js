import React from 'react';
import { NavBar } from '../components/NavBar';


export const Dashboard = () => {
    return (
        <div>
            <NavBar />
            <h1>Dashboard</h1>
        </div>

    );
}